Instructions to start server on a deployed instance.

If new instance:
- Update OS
- install node, git, npm
- Git clone repo into vm
- npm i dependencies.

Connect with database on separate instance:
- Check inbound rules for db instance
